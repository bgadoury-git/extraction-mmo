apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: extraction-mmo
spec:
  clusterIP: None   # headless — required by the StatefulSet governing service
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
